#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../gify/bin/gify" "$@"
  ret=$?
else 
  node  "$basedir/../gify/bin/gify" "$@"
  ret=$?
fi
exit $ret
